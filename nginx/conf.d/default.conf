# Default server - catch all undefined hosts
server {
    listen 80 default_server;
    server_name _;

    location / {
        return 404;
    }
}

# Include all virtual host configurations generated from database
include /etc/nginx/vhosts.d/*.conf;
